import { useEffect, useState } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { createGift, getGift, updateGift } from '../services/api';
const initial={title:'',description:'',category:'Living',condition:'Good',image:'https://images.unsplash.com/photo-1594026112284-02bb6f3352fe?auto=format&fit=crop&w=900&q=80',location:'Islamabad'};
export default function GiftForm(){const {id}=useParams();const edit=!!id;const [form,setForm]=useState(initial);const [error,setError]=useState('');const navigate=useNavigate();useEffect(()=>{if(edit)getGift(id).then(g=>setForm({title:g.title,description:g.description,category:g.category,condition:g.condition,image:g.image,location:g.location})).catch(e=>setError(e.message));},[id,edit]);const submit=async e=>{e.preventDefault();try{if(edit)await updateGift(id,form);else await createGift(form);navigate(edit?`/gifts/${id}`:'/gifts');}catch(e){setError(e.message);}};return <main className="page form-page"><div className="page-heading"><div><span className="eyebrow">{edit?'EDIT LISTING':'SHARE SOMETHING'}</span><h1>{edit?'Edit gift':'Add a gift'}</h1></div></div><form className="panel large-form" onSubmit={submit}><label>Title<input required value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/></label><label>Description<textarea required rows="5" value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></label><div className="two-col"><label>Category<select value={form.category} onChange={e=>setForm({...form,category:e.target.value})}>{['Living','Kitchen','Furniture','Bedroom','Electronics','Books'].map(x=><option key={x}>{x}</option>)}</select></label><label>Condition<select value={form.condition} onChange={e=>setForm({...form,condition:e.target.value})}>{['New','Like New','Good','Older'].map(x=><option key={x}>{x}</option>)}</select></label></div><div className="two-col"><label>Location<input value={form.location} onChange={e=>setForm({...form,location:e.target.value})}/></label><label>Image URL<input required value={form.image} onChange={e=>setForm({...form,image:e.target.value})}/></label></div>{error&&<div className="form-error">{error}</div>}<button className="primary-btn">{edit?'Save Changes':'Publish Gift'}</button></form></main>}
